---
title: Fleet
status: shipped
order: 1
role: Solo — design, build, ship
stack:
  - Containers
  - gateway proxy
  - git worktrees
summary: An open-source tool that runs every feature branch of your app at the same time on localhost, each in its own container behind a gateway.
statusLabel: Open source · shipped
links:
  github: https://github.com/ilies-bel/fleet
  npm: "@ilies-bel/fleet"
media:
  kind: image
  # Filename under src/assets/projects/ — resolved by projects.astro's
  # import.meta.glob (never the image() helper; see contentSchemas.ts).
  image: fleet.png
  alt: Fleet dashboard showing multiple feature branches running in parallel containers
  caption: "Fleet: parallel branch preview dashboard"
claims:
  - statement: It has users among the niche it was built for — engineers doing parallel QA.
    evidence:
      # EVIDENCE DEBT (draftEvidence gate): adoption is asserted from direct
      # conversations, not from a metric or testimonial captured anywhere.
      # Attach npm download counts / a named user quote, then flip
      # draftEvidence to false. Until then the build only passes because the
      # debt is flagged explicitly.
      type: none
limitations:
  - Niche by design — it only earns its keep when you review several branches in parallel; a one-branch-at-a-time workflow gains nothing.
  - Stack auto-detection covers the common setups; an exotic build still needs manual container config.
draftEvidence: true
---

An open-source tool that runs every feature branch of your app at the same time on localhost, each in its own container behind a gateway. One dashboard to spin them up, tail logs, preview the running app, diff UI states, and leave QA notes per feature. It auto-detects the stack and manages multiple repos through git worktrees.

I built it because reviewing several branches at once meant constantly tearing down and rebuilding one local environment. Fleet just runs them all in parallel and lets you flip between them.

<p class="projects-proud"><span class="projects-proud-label">What I am proud of:</span> it is a real tool, not a demo, and it found its people. It is niche by design, built for engineers doing parallel QA, and it has users among them. The container orchestration, the gateway proxy, the stack auto-detection, and the diff view all had to work together, and they do.</p>
