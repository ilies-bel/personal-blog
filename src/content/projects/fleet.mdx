---
title: Fleet
status: shipped
order: 1
role: Solo — design, build, ship
stack:
  - Containers
  - gateway proxy
  - git worktrees
summary: An open-source tool that runs every feature branch of your app at the same time on localhost, each in its own container behind a gateway.
statusLabel: Open source · shipped
links:
  github: https://github.com/ilies-bel/fleet
  npm: "@ilies-bel/fleet"
media:
  kind: image
  # Filename under src/assets/projects/ — resolved by CaseStudy.astro's
  # import.meta.glob (never the image() helper; see contentSchemas.ts).
  image: fleet.png
  alt: Fleet dashboard showing multiple feature branches running in parallel containers
  caption: "Fleet: parallel branch preview dashboard"
claims:
  - statement: It has users among the niche it was built for — engineers doing parallel QA.
    evidence:
      # EVIDENCE DEBT (draftEvidence gate): adoption is asserted from direct
      # conversations, not from a metric or testimonial captured anywhere.
      # Attach npm download counts / a named user quote, then flip
      # draftEvidence to false. Until then the build only passes because the
      # debt is flagged explicitly.
      type: none
# The honest engineering record (P13 — rendered by CaseStudy.astro as
# "Decisions & consequences"). Everything here restates what the entry's own
# prose already said; nothing is invented history.
rejectedPaths:
  - One shared local environment, torn down and rebuilt per branch — the workflow Fleet replaced. Rebuilding for every review is exactly the cost the tool exists to delete.
limitations:
  - Niche by design — it only earns its keep when you review several branches in parallel; a one-branch-at-a-time workflow gains nothing.
  - Stack auto-detection covers the common setups; an exotic build still needs manual container config.
currentStatus: Shipped and open source — the code and the npm package are public.
# HOME FEATURE PROOF (PRD-003) — this is the single project featured on the hero's
# yellow-star Projects marker. Facts only: the parallel-local-branch purpose, and
# signals backed by this entry's own status/links (open source, shipped, the npm
# package). No adoption/metric claim (that debt is tracked in `claims` under
# draftEvidence). getFeaturedProof() enforces at most ONE public feature site-wide.
heroProof:
  feature: true
  summary: Parallel branch environments, locally.
  signals:
    - Open source
    - Shipped
draftEvidence: true
---

### The problem

Reviewing several feature branches at once meant constantly tearing down and rebuilding one local environment. Every switch threw away a working setup, and the rebuild ate the time the review was supposed to save.

### The decisive choice

Stop rebuilding and run them all: every feature branch of your app at the same time on localhost, each in its own container behind a gateway. Fleet auto-detects the stack and manages multiple repos through git worktrees.

### The outcome

One dashboard to spin branches up, tail logs, preview the running app, diff UI states, and leave QA notes per feature — you flip between branches instead of rebuilding one. The container orchestration, the gateway proxy, the stack auto-detection, and the diff view all had to work together, and they do. It is a real tool, not a demo.
