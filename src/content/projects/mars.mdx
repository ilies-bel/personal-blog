---
title: Mars
status: private
order: 2
role: Solo — design, build, ship
stack:
  - Multi-agent orchestration
  - task graph
summary: A local AFK agent swarm for software work — give it a goal and it builds, orders, and executes a web of tasks autonomously.
statusLabel: Private · in use
privateNote: B2B product. No public link.
media:
  kind: diagram
  diagram: mars-topology
  caption: "Mars: agent task topology — tasks coloured by lifecycle state, edges are blocker dependencies."
claims:
  - statement: Mars is in production use with its client.
    evidence:
      # EVIDENCE DEBT (draftEvidence gate): "in use" is asserted with nothing a
      # visitor can check — the product is private, so the honest backing is a
      # client testimonial or a dated usage note. Capture one, then flip
      # draftEvidence to false.
      type: none
limitations:
  - Private by contract — there is no public demo, so every capability claim rests on this page's word.
  - Recovery handles failed tasks, not failed PLANS — a mis-decomposed goal still needs a human to restate it.
draftEvidence: true
---

A local AFK agent swarm for software work. You give it a goal and it builds a web of tasks, figures out the order they have to run in, and executes the whole graph autonomously: writing code, recovering from failures, and chaining the follow-up work without a human in the loop. The topology view lets you watch the dependency web plan and run itself in real time.

It is the most ambitious thing I have built. Coordinating many agents so they do the right work in the right order, and recover when a step fails, is a genuinely hard orchestration problem, and Mars does it.

<p class="projects-proud"><span class="projects-proud-label">Note:</span> a private product, so there is no public link. The diagram above mirrors the live topology view — nodes are tasks, edges are blocker dependencies, colours are lifecycle state, and the failed branch shows a recovered task picked up by a follow-up agent.</p>
