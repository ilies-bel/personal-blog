import cockpitPlateUrl from '../assets/cockpit/cockpit-plate.png?url';

/** Fixed authoring space shared by the raster plate, live overlay and HUD. */
export const COCKPIT_W = 1920;
export const COCKPIT_H = 1080;

/** The one runtime cockpit silhouette, shared by live and reduced motion. */
export const COCKPIT_PLATE_URL = cockpitPlateUrl;
