{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r \".tool_input.command\" | grep -Eq \"(^|[;&|]|&&|\\|\\|)[[:space:]]*git[[:space:]]+switch([[:space:]]|$)\" && printf \"%s\" \"{\\\"hookSpecificOutput\\\":{\\\"hookEventName\\\":\\\"PreToolUse\\\",\\\"permissionDecision\\\":\\\"deny\\\",\\\"permissionDecisionReason\\\":\\\"git switch is disabled in this repo. Use git checkout instead.\\\"}}\" || true",
            "statusMessage": "Checking for git switch"
          }
        ]
      }
    ]
  }
}
