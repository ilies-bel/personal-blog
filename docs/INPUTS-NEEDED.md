# Inputs needed from the site owner

The SOTY execution program (see `docs/roadmaps/soty-execution-plan.md`)
implements everything code can implement. The items below are blocked on
information, permissions, accounts, or sessions only the owner can supply.
Each row names what unblocks when the input lands.

| # | Input | Unblocks | Where it plugs in |
|---|-------|----------|-------------------|
| 1 | Git remote / GitHub repo access confirmed for this branch | CI proven green on GitHub, staging, scheduled prod crawl, deploys | `.github/workflows/ci.yml` runs on push once the branch is on GitHub |
| 2 | Fleet adoption evidence — npm download stats, GitHub stars/issues/PRs, dated screenshots, or user quotes | De-hedging the Fleet "has users" claim | `src/content/projects/fleet.mdx` → `claims[].evidence` (replace `type: none` + remove `draftEvidence`) |
| 3 | Mars publication permission + a sanitized real task graph, one real failure/recovery story, scale numbers, measured outcome | Mars redacted case study | `src/content/projects/mars.mdx` claims + body; mark real vs reconstructed vs redacted |
| 4 | A third publicly inspectable flagship project (different capability than Fleet/Mars) with artifacts: sketches, issues, commits, profiler captures, failed states | Three-flagship requirement; finale counts update automatically | New `src/content/projects/<name>.mdx` — schema validates it |
| 5 | 2–3 specific testimonials with publication permission | Evidence sections on case studies | `claims[].evidence.type: testimonial` |
| 6 | Real availability data: status, note, location, timezone, preferred engagements, response expectation | Honest /contact page (safe defaults ship today) | `AVAILABILITY` in `src/consts.ts` |
| 7 | Graveyard specimens 3–6, 3–5 planned articles, an approved master biography | Editorial depth (roadmap §5) | `src/content/graveyard/*.mdx`, `src/content/posts/*.mdx`, about page |
| 8 | Real-device testing (iPhone, Pixel, low-tier Android, 5-minute thermal runs), NVDA/VoiceOver/TalkBack sessions, first-time usability sessions (≥5 users), disability-led sessions, 9-person blind proxy jury | Validation sign-offs the emulated CI matrix cannot provide (roadmap §7) | Session scripts to be generated in P14 |
| 9 | Cloudflare dashboard actions: paste the generated response-header rules (CSP/HSTS/etc.), optionally enable Web Analytics, optionally CF Pages previews for staging | Security headers live in production | Rule values generated by `scripts/gen-csp-hashes.mjs` → `docs/SECURITY-HEADERS.md` (P11) |
| 10 | Awwwards account, category/tags choices, submission window decision, fees; ethical-voting outreach list | Campaign execution (roadmap §9) | P12 landed the code side: templates in `docs/awwwards/` (narratives, Elements captions, press kit, voting policy, dev-award dossier, dry-run checklist), route OG cards in `public/og/`, capture/reel tooling (`scripts/shoot-submission.mjs`, `scripts/record-reel.mjs` — rerun on real GPU hardware for the actual uploads) |

## Flagged evidence debt currently in the content

Both existing projects carry `draftEvidence: true` — the build-time schema
(`src/lib/contentSchemas.ts`) fails any claim with `evidence.type: 'none'`
unless that flag is set, so the debt is explicit and greppable:

- `fleet.mdx` — adoption claim (needs input #2)
- `mars.mdx` — "in production use" claim (needs inputs #3/#5)

Retiring the flags is the acceptance criterion for inputs #2/#3/#5.
