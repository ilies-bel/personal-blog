# pnpm: use a flat, copied node_modules (no symlinks) so each git worktree gets
# its own self-contained dependency tree. Symlinked layouts broke Vite's
# server.fs root across worktrees and hurt dev performance.
node-linker=hoisted
# Copy package files from the store instead of hard/sym-linking them.
package-import-method=copy
